services:
  crm-app:
    build: .
    container_name: crm-backend
    restart: always # Alterado para always para garantir recuperação automática
    environment:
      SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_ISSUER_URI: ${KC_EXTERNAL_URL}/realms/crm-realm
      SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_JWK_SET_URI: ${KC_INTERNAL_URL}/realms/crm-realm/protocol/openid-connect/certs
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/${POSTGRES_DB}
      SPRING_DATASOURCE_USERNAME: ${POSTGRES_USER}
      SPRING_DATASOURCE_PASSWORD: ${POSTGRES_PASSWORD}
      KEYCLOAK_REALM: crm-realm
      KEYCLOAK_CLIENT_ID: crm-backend
      KEYCLOAK_CLIENT_SECRET: ${KEYCLOAK_CLIENT_SECRET}
      KEYCLOAK_AUTH_SERVER_URL: http://keycloak:8080
      CAKTO_WEBHOOK_SECRET: ${CAKTO_WEBHOOK_SECRET}
      CAKTO_PRODUCT_ID: ${CAKTO_PRODUCT_ID}
      MAIL_USER: ${MAIL_USER}
      MAIL_PASS: ${MAIL_PASS}
    # REMOVIDO: portas expostas. O Coolify usa o domínio para chegar aqui.
    depends_on:
      postgres:
        condition: service_healthy
      keycloak:
        condition: service_started
    networks:
      - crm-network

  postgres:
    image: postgres:16
    container_name: crm-postgres
    restart: always
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      # Agora o banco só é acessível de dentro da VPS (via tunel SSH se precisar)
      - "127.0.0.1:2001:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}" ]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - crm-network

  keycloak:
    image: quay.io/keycloak/keycloak:24.0
    container_name: crm-auth
    restart: always
    command: start-dev
    environment:
      KC_DB: postgres
      KC_DB_URL: jdbc:postgresql://postgres:5432/${POSTGRES_DB}
      KC_DB_USERNAME: ${POSTGRES_USER}
      KC_DB_PASSWORD: ${POSTGRES_PASSWORD}
      KEYCLOAK_ADMIN: ${KC_ADMIN_USER}
      KEYCLOAK_ADMIN_PASSWORD: ${KC_ADMIN_PASSWORD}
      KC_HOSTNAME: ${KC_DOMAIN}
      KC_HOSTNAME_STRICT: "false"
      KC_HTTP_ENABLED: "true"
      KC_PROXY: edge
    # O Coolify usa o domínio configurado para o Keycloak, não precisa expor a 2002
    networks:
      - crm-network

  adminer:
    image: adminer
    container_name: crm-adminer
    restart: always
    # O Adminer também deve ser acessado pelo domínio gerado pelo Coolify
    networks:
      - crm-network

networks:
  crm-network:
    driver: bridge

volumes:
  postgres_data: